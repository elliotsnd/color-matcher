# Python dependencies for ESP32 Color Matcher project
# Install with: pip install -r requirements.txt

# Core dependencies for build scripts
platformio>=6.0.0

# Optional dependencies for development
# (These are only needed if you want to run the Python scripts)

# For JSON processing and data manipulation
# (Usually included with Python, but listed for completeness)

# For file operations and utilities
# (Standard library modules, no additional packages needed)

# Development and testing tools (optional)
# pytest>=7.0.0          # For running tests
# black>=22.0.0          # For code formatting
# flake8>=4.0.0          # For code linting

# Note: Most scripts use only Python standard library
# The main requirement is PlatformIO for ESP32 development
