# Cppcheck configuration for Color Sensor ESP32 Project
# This file contains suppressions for false positives and project-specific settings

# Suppress system include warnings (Arduino libraries)
missingIncludeSystem

# Suppress unused function warnings (Arduino setup/loop functions might appear unused)
unusedFunction:*setup*
unusedFunction:*loop*

# Suppress unmatched suppressions (in case some rules don't apply)
unmatchedSuppression

# Arduino/ESP32 specific suppressions
# These are common patterns in embedded Arduino code that Cppcheck might flag incorrectly

# String concatenation with + operator (common in Arduino)
# unusedVariable:*String*

# Serial print statements that might appear to have unused expressions
# constStatement:*Serial*

# WiFi and networking related suppressions (async operations)
# unusedVariable:*WiFi*
# unusedVariable:*AsyncWeb*

# JSON library suppressions (ArduinoJson uses some advanced C++ patterns)
# cppcheckError:*ArduinoJson*

# Hardware register access (ESP32 specific)
# nullPointer:*GPIO*
# nullPointer:*SPI*
# nullPointer:*I2C*

# File-specific suppressions
# Add specific suppressions here if needed, e.g.:
# unusedFunction:src/main.cpp:specific_function_name

# Example of how to suppress specific warnings in specific files:
# [src/main.cpp]
# unusedVariable
# constStatement

# Performance-related suppressions for embedded code
# Sometimes embedded code prioritizes functionality over performance optimization warnings
# passedByValue  # May want to pass by reference for performance, but sometimes passing by value is fine for small objects

# Memory allocation suppressions (PSRAM usage in ESP32)
# These might be flagged but are intentional for ESP32 PSRAM usage
# memleakOnRealloc
# nullPointerRedundantCheck

# Add any project-specific suppressions below:
# Format: rule_name:file_pattern:line_or_function
